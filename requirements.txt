# Ultra-minimal requirements for Render deployment
# Developed by Maksim Kitikov - Upside Analytics

# Core web framework (stable versions)
dash==2.14.1
plotly==5.15.0
gunicorn==20.1.0

# Database
psycopg2-binary==2.9.7

# Basic utilities
python-dotenv==1.0.0
requests==2.31.0

# Removed problematic packages:
# - pandas (requires numpy compilation)
# - numpy (C compilation issues)  
# - scikit-learn (requires numpy/scipy)
# - SQLAlchemy (can cause issues)
# - us (not essential)